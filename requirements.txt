# Core dependencies for micalign (the alignment path) + the vendored MicaSense library.
numpy>=1.23
opencv-python>=4.5
tifffile>=2021.7
imageio>=2.9
scikit-image>=0.19
matplotlib>=3.3
pyexiftool>=0.5
pytz>=2021.1
pysolar>=0.10
pyzbar>=0.1.9
packaging>=20.0

# Optional — imported LAZILY by the vendored MicaSense library, inside functions
# that the alignment path never calls, so they are NOT needed for micalign:
#   rawpy       — only for reading raw/DNG camera files
#   gdal/osgeo  — only for exporting georeferenced GeoTIFFs
